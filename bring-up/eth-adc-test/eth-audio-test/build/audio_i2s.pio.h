// ---------------------------------------------------------------- //
// This file is autogenerated by pioasm version 2.2.0; do not edit! //
// ---------------------------------------------------------------- //

#pragma once

#if !PICO_NO_HARDWARE
#include "hardware/pio.h"
#endif

// --------- //
// audio_i2s //
// --------- //

#define audio_i2s_wrap_target 0
#define audio_i2s_wrap 21
#define audio_i2s_pio_version 1

#define audio_i2s_offset_entry_point 0u

static const uint16_t audio_i2s_program_instructions[] = {
            //     .wrap_target
    0x2094, //  0: wait   1 gpio, 20
    0x2014, //  1: wait   0 gpio, 20
    0x2093, //  2: wait   1 gpio, 19
    0x2013, //  3: wait   0 gpio, 19
    0xe037, //  4: set    x, 23
    0x2093, //  5: wait   1 gpio, 19
    0x4001, //  6: in     pins, 1
    0x2013, //  7: wait   0 gpio, 19
    0x0045, //  8: jmp    x--, 5
    0x4068, //  9: in     null, 8
    0x8000, // 10: push   noblock
    0x2094, // 11: wait   1 gpio, 20
    0x2093, // 12: wait   1 gpio, 19
    0x2013, // 13: wait   0 gpio, 19
    0xe037, // 14: set    x, 23
    0x2093, // 15: wait   1 gpio, 19
    0x4001, // 16: in     pins, 1
    0x2013, // 17: wait   0 gpio, 19
    0x004f, // 18: jmp    x--, 15
    0x4068, // 19: in     null, 8
    0x8000, // 20: push   noblock
    0x0000, // 21: jmp    0
            //     .wrap
};

#if !PICO_NO_HARDWARE
static const struct pio_program audio_i2s_program = {
    .instructions = audio_i2s_program_instructions,
    .length = 22,
    .origin = -1,
    .pio_version = audio_i2s_pio_version,
#if PICO_PIO_VERSION > 0
    .used_gpio_ranges = 0x2
#endif
};

static inline pio_sm_config audio_i2s_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + audio_i2s_wrap_target, offset + audio_i2s_wrap);
    return c;
}

static inline void audio_i2s_program_init(PIO pio, uint sm, uint offset, uint data_pin, uint bclk_pin, uint ws_pin) {
    pio_gpio_init(pio, data_pin);
    pio_gpio_init(pio, bclk_pin);
    pio_gpio_init(pio, ws_pin);
    gpio_disable_pulls(data_pin);
    gpio_disable_pulls(bclk_pin);
    gpio_disable_pulls(ws_pin);
    pio_sm_config c = audio_i2s_program_get_default_config(offset);
    sm_config_set_in_pins(&c, data_pin);
    sm_config_set_in_shift(&c, false, false, 32);
    sm_config_set_fifo_join(&c, PIO_FIFO_JOIN_RX);
    pio_sm_init(pio, sm, offset, &c);
    uint32_t pin_mask = (1u << data_pin) | (1u << bclk_pin) | (1u << ws_pin);
    pio_sm_set_pindirs_with_mask(pio, sm, 0, pin_mask);
}

#endif

