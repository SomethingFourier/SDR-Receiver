--- quisk_hardware_rp2350.py
+++ quisk_hardware_rp2350.py
@@ -10,7 +10,8 @@
 
 class Hardware(BaseHardware):
     def __init__(self, app, conf):
-        print("RP2350 HARDWARE INIT CALLED!")
+        with open("/tmp/quisk_debug.txt", "w") as f:
+            f.write("RP2350 HARDWARE INIT CALLED!\n")
         BaseHardware.__init__(self, app, conf)
         
         # Configure Quisk to expect 3-byte (24-bit) little-endian samples
@@ -22,7 +23,8 @@
         self.bind_ip = getattr(self.conf, "bind_ip", "0.0.0.0")
 
     def open(self):
-        print("RP2350 HARDWARE OPEN CALLED!")
+        with open("/tmp/quisk_debug.txt", "a") as f:
+            f.write("RP2350 HARDWARE OPEN CALLED!\n")
         # Open the UDP socket
         try:
             self.sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
