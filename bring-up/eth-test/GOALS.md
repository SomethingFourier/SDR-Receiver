# Goals

For this testing firmware, we aim to prove that the Ethernet hardware is functional by implementing an Ethernet stack in firmware and building a very simple web interface, such as a simple hello world page that can be accessed over the Ethernet connection.
